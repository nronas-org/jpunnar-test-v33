import { Meta, Preview, Story } from '@storybook/addon-docs'
import OrderHistory from '.'
import getDecorators from './decorators'

<Meta title="Components/OrderHistory" parameters={{ layout: 'padded' }} />

# OrderHistory

The `OrderHistory` component is used to display the customer's orders history.

## Examples

### Shopify

<Preview>
  <Story
    name="Shopify"
    decorators={getDecorators('john@shopify-platform.com')}
    parameters={{
      docs: {
        source: {
          code: '<OrderHistory orders={orders} />',
        },
      },
    }}
  >
    <OrderHistory />
  </Story>
</Preview>

### BigCommerce

<Preview>
  <Story
    name="BigCommerce"
    decorators={getDecorators('bill@bigcommerce-platform.com')}
    parameters={{
      docs: {
        source: {
          code: '<OrderHistory orders={orders} />',
        },
      },
    }}
  >
    <OrderHistory />
  </Story>
</Preview>
